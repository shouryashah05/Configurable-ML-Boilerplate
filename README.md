# ML Boilerplate

This repository contains a flexible base/pseudo-code framework designed for rapid experimentation with different models and parameters. The core execution logic remains static; users only need to swap in their desired model definitions and configuration settings.
